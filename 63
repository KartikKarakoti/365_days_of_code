#include <cs50.h>
#include <stdio.h>
void draw(int n);
int main (void)
{
    int height = get_int("Height: ")
        draw (height);
}

void draw (int n)
{
    draw (n - 1);

if (n <= 0)
{
    return;
}
