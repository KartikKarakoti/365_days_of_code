#include <cs50.h>
#include <stdio.h>
void draw(int n);
int main (void)
{
    int height = get_int("Height: ")
    drw (height)'
}

void draw (int n);
{
    for (int i = 0; 1< n; i++)
    {
         for (int j = 0; j<1=1; j++)
    }
         print f("#");
}
